#!/usr/bin/env node
var debug = require('debug')('SimpleTODO');
var app = require('../app');

var port = process.env.PORT || 3000; // socket.io

app.listen(port, function() { // socket.io
  debug('Express server listening on port ' + app.address().port);
});
